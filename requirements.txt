# Pinned as requested to maintain specific version
crewai==0.130.0

# Main libraries without version pins to allow for conflict resolution
crewai-tools
fastapi
uvicorn
langchain
langchain-google-genai
langchain-community
embedchain
python-dotenv